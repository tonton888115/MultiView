{
  "name": "multiview-youtube-extractor",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that proxies YouTube InnerTube calls for MultiView",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
