name = "multiview"
main = "src/index.js"
compatibility_date = "2026-05-26"
compatibility_flags = ["nodejs_compat"]

[observability]
enabled = true

# 既存 worker URL https://multiview.rinngo0626.workers.dev に上書きデプロイされる。
# free tier defaults: 100,000 requests/day, no payment required
#
# iOS InnerTube player response から live/DVR の HLS manifest URL を返す。
# VOD の signature cipher 解読や DRM には対応しない。
